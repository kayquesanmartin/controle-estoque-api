spring:
  application:
    name: API de Controle de Estoque
  datasource:
    url: jdbc:h2:mem:produtos
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      path: /h2-console
      enabled: true